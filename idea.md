# 학교 시험 문제 만들기 — 화면 및 핵심 기능 설계서

## 1. 프로젝트 개요

### 목적
학교 시험 대비를 위해 학생이 **학년 → 과목 → 단원**을 선택하면 해당 범위의 문제를 풀 수 있는 웹 서비스를 만든다.

### 기본 방향
- 처음에는 기능을 단순하게 구성한다.
- 실제 문제에 사용할 교과서와 단원 정보는 추후 정한다.
- 문제 데이터가 준비되면 단원별 문제를 충분히 저장하고, 그중 일부를 랜덤으로 출제한다.
- 학생이 문제를 풀고 바로 정답과 해설을 확인할 수 있게 한다.

---

## 2. 전체 화면 흐름

```text
[메인/설정 화면]
       │
       ├─ 학년 선택
       ↓
       ├─ 과목 선택
       ↓
       ├─ 단원 선택
       ↓
       ├─ 문제 수 선택
       ↓
       └─ 문제 만들기
              │
              ↓
       [문제 풀이 화면]
              │
              ├─ 문제 표시
              ├─ 답 선택/입력
              ├─ 정답 확인
              └─ 다음 문제
                     │
                     ↓
               [결과 화면]
                     │
              ├─ 점수 확인
              ├─ 맞힌 문제 수
              ├─ 틀린 문제 수
              ├─ 다시 풀기
              └─ 다른 단원 선택
```

---

## 3. 화면 설계

## 3-1. 메인 / 문제 설정 화면

### 화면 목적
학생이 문제를 풀 범위를 선택하는 첫 화면이다.

### 구성

#### ① 학년 선택
- 1학년
- 2학년
- 3학년

선택한 학년은 활성화 상태로 표시한다.

#### ② 과목 선택
예:
- 국어
- 수학
- 영어
- 과학
- 사회

단, 실제 과목은 나중에 대상 학교와 학년에 맞춰 정한다.

#### ③ 단원 선택
선택한 학년과 과목에 해당하는 단원만 표시한다.

예:

```text
1단원
2단원
3단원
4단원
5단원
```

단원 이름도 추후 사용할 교과서에 맞춰 변경한다.

#### ④ 문제 수
초기에는 다음 정도로 제공한다.

```text
5문제
10문제
15문제
```

단원에 실제 등록된 문제보다 많은 수를 선택했을 경우에는 같은 문제가 반복되지 않도록 하고, 문제를 더 추가하거나 문제 수를 줄이도록 안내한다.

#### ⑤ 문제 유형
초기 지원 유형:

```text
객관식
O / X
주관식
```

### 주요 버튼

```text
[ 문제 만들기 ]
```

모든 필수 선택이 완료된 경우에만 문제 풀이 화면으로 이동한다.

---

## 3-2. 문제 풀이 화면

### 화면 목적
선택한 단원의 문제를 한 문제씩 풀 수 있도록 한다.

### 상단 정보

```text
← 설정으로

2학년 · 수학 · 1단원

1 / 5
━━━━━━━━━━━━━━

점수 2
```

### 문제 영역

```text
문제 1

다음 중 올바른 것은?

① 보기 1
② 보기 2
③ 보기 3
④ 보기 4
```

### 답안 방식

#### 객관식
보기를 버튼 형태로 보여준다.

#### O / X
```text
[ O ]
[ X ]
```

#### 주관식
```text
[ 정답 입력                    ]
```

### 정답 확인

```text
[ 정답 확인 ]
```

정답 확인 후:

- 정답이면 정답 표시
- 오답이면 오답 표시
- 정답과 해설 표시
- 답안을 수정할 수 없도록 처리

### 다음 문제

정답 확인 이후:

```text
[ 다음 문제 ]
```

마지막 문제라면 결과 화면으로 이동한다.

---

## 3-3. 결과 화면

### 화면 목적
시험 결과를 한눈에 보여준다.

### 구성

```text
🎉 문제 풀이 완료!

        80점

5문제 중 4문제를 맞혔어요.

맞힌 문제     4
틀린 문제     1
선택 단원     1단원
```

### 버튼

```text
[ 다시 풀기 ]
[ 다른 선택하기 ]
```

#### 다시 풀기
같은 학년/과목/단원 설정으로 새로운 문제 조합을 만든다.

#### 다른 선택하기
설정 화면으로 돌아간다.

---

## 4. 핵심 기능

## 4-1. 학년 선택

선택한 학년을 기억한다.

예:

```javascript
selectedGrade = "2";
```

---

## 4-2. 과목 선택

선택한 학년에 맞는 과목만 표시한다.

예:

```text
2학년
→ 국어
→ 수학
→ 영어
```

---

## 4-3. 단원 선택

선택한 학년과 과목에 해당하는 단원만 표시한다.

예:

```text
2학년
→ 수학
→ 1단원
→ 2단원
→ 3단원
```

---

## 4-4. 랜덤 문제 출제

단원에 여러 문제가 등록되어 있다고 가정한다.

예:

```text
1단원
├─ 문제 1
├─ 문제 2
├─ 문제 3
├─ ...
├─ 문제 19
└─ 문제 20
```

학생이 5문제를 선택하면:

```text
20개 중 5개를 랜덤 선택
```

한다.

### 중요한 조건

문제 수보다 등록된 문제가 적으면 같은 문제를 반복해서 출제하지 않는다.

예:

```text
등록 문제: 3개
선택 문제 수: 5개
```

이 경우:

```text
문제가 부족합니다.
문제를 더 추가하거나 문제 수를 줄여 주세요.
```

라고 안내한다.

---

## 4-5. 정답 판정

각 문제는 정답 정보를 가지고 있어야 한다.

객관식 예:

```javascript
{
    type: "multiple",
    question: "5 + 3의 값은?",
    choices: ["6", "7", "8", "9"],
    answer: 2,
    explanation: "5 + 3 = 8입니다."
}
```

주관식 예:

```javascript
{
    type: "short",
    question: "대한민국의 수도는?",
    answerText: "서울",
    explanation: "대한민국의 수도는 서울입니다."
}
```

O/X 예:

```javascript
{
    type: "ox",
    question: "문장의 내용이 맞으면 O, 틀리면 X를 선택하세요.",
    oxAnswer: true,
    explanation: "정답은 O입니다."
}
```

---

## 4-6. 점수 계산

예를 들어 5문제 중 4문제를 맞혔으면:

```text
4 / 5 = 80점
```

계산식:

```javascript
score = Math.round((correct / total) * 100);
```

---

## 5. 문제 데이터 구조

전체 데이터는 다음과 같은 구조를 기본으로 한다.

```text
학년
 └─ 과목
     └─ 단원
         └─ 문제 목록
```

예:

```javascript
const examData = {
    "2": {
        "수학": {
            "1단원": [
                {
                    type: "multiple",
                    question: "문제 내용",
                    choices: [
                        "보기 1",
                        "보기 2",
                        "보기 3",
                        "보기 4"
                    ],
                    answer: 1,
                    explanation: "해설"
                }
            ]
        }
    }
};
```

이 구조를 사용하면 나중에 새로운 학년, 과목, 단원을 추가하기 쉽다.

---

## 6. 문제 추가 원칙

문제는 실제 사용할 교과서와 단원을 확인한 뒤 추가한다.

### 단원별 권장 문제 수

초기 목표:

```text
최소 10문제
권장 20~30문제 이상
```

문제가 많을수록 랜덤 출제 시 같은 문제를 만날 가능성이 줄어든다.

---

## 7. 사용자 경험 원칙

### 선택 과정
학생이 지금 무엇을 선택하고 있는지 항상 알 수 있어야 한다.

```text
1. 학년
2. 과목
3. 단원
4. 문제 수
5. 문제 만들기
```

### 문제 풀이
- 문제 번호를 보여준다.
- 전체 진행 상황을 보여준다.
- 이미 푼 문제와 현재 문제를 구분한다.
- 정답을 확인한 뒤 다음 문제로 이동한다.

### 결과
점수만 보여주지 않고 맞힌 문제와 틀린 문제도 함께 보여준다.

---

## 8. 개발 파일 구조

초기 버전은 다음 3개 파일로 구성한다.

```text
school-exam/
├── index.html
├── style.css
└── script.js
```

### index.html
화면의 구조를 담당한다.

### style.css
색상, 버튼, 카드, 반응형 화면 등 디자인을 담당한다.

### script.js
다음 기능을 담당한다.

- 학년 선택
- 과목 선택
- 단원 선택
- 문제 출제
- 랜덤 선택
- 답안 처리
- 정답 판정
- 점수 계산
- 결과 표시

---

## 9. 개발 우선순위

### 1단계 — 화면
- 학년 선택
- 과목 선택
- 단원 선택
- 문제 수 선택
- 문제 만들기 버튼

### 2단계 — 문제 풀이
- 문제 표시
- 객관식 답안
- 정답 확인
- 다음 문제

### 3단계 — 결과
- 점수
- 맞힌 문제 수
- 틀린 문제 수
- 다시 풀기

### 4단계 — 문제 데이터 확장
- 실제 교과서 확인
- 단원별 문제 추가
- 문제 수 확대

### 5단계 — 추가 기능
추후 필요할 경우 다음 기능을 추가한다.

- 오답노트
- 틀린 문제 다시 풀기
- 문제 즐겨찾기
- 학습 기록
- 문제 난이도
- 제한 시간
- 실제 새로운 문제 생성 기능

---

## 10. 현재 단계의 목표

지금은 기능을 너무 많이 넣기보다 다음 기능이 **정확하게 작동하는 것**을 우선한다.

```text
학년 선택
   ↓
과목 선택
   ↓
단원 선택
   ↓
5문제 선택
   ↓
문제 풀이
   ↓
정답 확인
   ↓
결과 확인
```

실제 사용할 책과 교과서 정보를 정한 후, 해당 정보에 맞춰 단원과 문제 데이터를 넣는다.

---

## 11. 추후 확장 방향

교과서와 문제가 준비되면 단원별 문제를 충분히 저장하고, 시험을 시작할 때마다 해당 단원의 문제 중 일부를 랜덤으로 선택하도록 한다.

향후 AI 문제 생성 기능을 도입할 경우에는 다음과 같은 구조로 확장할 수 있다.

```text
학년
 ↓
과목
 ↓
단원
 ↓
문제 생성 요청
 ↓
새로운 문제 생성
 ↓
문제 풀이
 ↓
채점
```

단, AI 문제 생성 기능은 초기 버전이 안정적으로 작동한 뒤 추가하는 것을 권장한다.
